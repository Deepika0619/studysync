<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= noTitle ? '' : (title || 'StudySync') %></title>

  <link rel="stylesheet" href="/styles.css" />
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #1f222b;
      color: #fff;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      background: #1f222b;
      color: white;
      padding: 1.5rem 2rem;
      display: flex;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    header h1 {
      font-size: 1.8rem;
      margin: 0;
    }

    main {
      flex: 1;
      padding: 2rem;
    }

    footer {
      background: #1f222b;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
      color: #aaa;
      border-top: 1px solid #333;
    }

    footer strong {
      color: #fff;
    }
  </style>
</head>
<body>
  <header>
    <h1>StudySync</h1>
  </header>

  <main>
    <%- body %>
  </main>

  <% if (!noFooter) { %>
    <footer>
      <p>© 2025 <strong>StudySync</strong> — Made by Deepika Annapaneni</p>
    </footer>
  <% } %>

</body>
</html>